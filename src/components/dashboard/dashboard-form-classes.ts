/** Shared native control styling for dashboard forms (matches Card / input tokens). */
export const DASHBOARD_NATIVE_SELECT_CLASS =
  "rounded-lg border border-border bg-background px-2.5 py-1.5 text-sm text-foreground shadow-[var(--elevation-1)] transition-[box-shadow,border-color] duration-200 hover:border-muted-foreground/40 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 focus:ring-offset-background";

/** Form labels in post editor and similar CMS screens (premium light, semantic tokens). */
export const DASHBOARD_FORM_LABEL_CLASS = "text-sm font-medium tracking-[-0.01em] text-foreground";

/** Native checkbox styling consistent with dashboard inputs. */
export const DASHBOARD_NATIVE_CHECKBOX_CLASS =
  "h-4 w-4 rounded border-border accent-primary cursor-pointer";
