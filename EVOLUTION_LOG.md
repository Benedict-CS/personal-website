# Evolution log

Append-only record of autonomous upgrade cycles. Each line is one cycle.

[2026-04-06] D (Reliability / Infra): Dashboard `error.tsx` now surfaces Next.js `error.digest` when present, with a copy-to-clipboard control and toast feedback, aligned with the root `global-error` reference pattern.
[2026-04-06] B (CMS core): Dashboard note detail (`/dashboard/notes/[slug]`) passes server-parsed `initialHeadings` into `TableOfContents`, matching the public blog TOC pattern for faster first paint and mobile drawer content.
[2026-04-06] A (UX polish): Dashboard shell route transition, command palette, and keyboard-help modal use Framer Motion `useReducedMotion` so `prefers-reduced-motion: reduce` skips enter/exit motion and layout animation on the palette panel.
[2026-04-06] D (Reliability / Infra): GitHub Actions CI installs Playwright Chromium (`install --with-deps`) and runs `npm run test:e2e` after verify, using `PLAYWRIGHT_SKIP_BUILD=1` so the web server reuses the existing production build instead of building twice.
[2026-04-06] B (CMS core): Blog draft preview (`/blog/preview` with token) passes server-parsed `initialHeadings` into `TableOfContents`, matching published post and dashboard note behavior for faster TOC paint and mobile drawer content.
[2026-04-06] B (CMS core): Blog preview `TableOfContents` now receives `initialHeadings={tocHeadings}` so server-parsed headings are used (previously only `content` was passed).
[2026-04-06] A (UX polish): Markdown AST lab (`ast-lab-client`) uses Framer Motion `useReducedMotion` to disable layout animation on the editor panel and skip tree / error fade transitions when `prefers-reduced-motion: reduce` is set.
[2026-04-06] D (Reliability / Infra): `PATCH /api/site-config` normalizes unknown JSON into string-or-null and `Prisma.InputJsonValue` for create/update; GitHub integration tests mock `fetch` with `RequestInfo | URL` so typecheck matches the standard `fetch` signature.
[2026-04-07] B (CMS core): Dashboard posts list shows `Intl.RelativeTimeFormat` for created/updated columns (desktop table and mobile cards) with full locale date+time in the `title` tooltip for precision.
[2026-04-07] A (UX polish): Global `ToastProvider` uses Framer Motion `useReducedMotion` so toast enter/exit skips slide and scale when `prefers-reduced-motion: reduce` is set (light styling unchanged).
[2026-04-07] A (UX polish): Shared `MotionCard` (dashboard overview) respects `useReducedMotion`: no stagger delay, no vertical fade-in, and no hover scale when reduced motion is preferred.
[2026-04-07] C (Developer / geek): Extracted `formatRelativeTime` / `formatAbsoluteDateTime` to `src/lib/relative-time.ts` with optional `nowMs` for tests; dashboard posts table imports the shared helpers; added Jest coverage in `relative-time.test.ts`.
[2026-04-07] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) now covers `GET` and `HEAD` `/api/v1/health` — JSON includes `version: "v1"`, `Cache-Control: no-store`, and HEAD mirrors GET status with an empty body.
[2026-04-07] B (CMS core): Dashboard media library adds a persisted grid sort (newest/oldest, name A–Z/Z–A, size) with `src/lib/media-grid-sort.ts`, `localStorage` key `media-library-grid-sort-v1`, and Jest coverage; keyboard help documents Home/End in the media grid.
[2026-04-08] A (UX polish): Dashboard keyboard help toggles on ⌘/ or Ctrl+/ (plus `e.code === "Slash"` for layout safety) when focus is not in a text field; shortcut list and subtitle document the chord alongside `?`.
[2026-04-08] C (Developer / geek): Dashboard overview `System status` runs `/api/health` and `/api/live` in parallel with per-request RTT, surfaces liveness vs readiness labels, adds a diagnostics explainer for DB vs process latency, and includes liveness in copied diagnostics.
[2026-04-09] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) now covers `GET` and `HEAD` `/api/live` — JSON `{ ok: true, live: true }`, `Cache-Control: no-store`, and HEAD mirrors GET with an empty body (aligns with dashboard liveness checks).
[2026-04-09] B (CMS core): Dashboard notes index cards show `Updated` + `Intl.RelativeTimeFormat` via `formatRelativeTime`, with precise `formatAbsoluteDateTime` in the `title` tooltip (same pattern as the posts list).
[2026-04-10] A (UX polish): Keyboard shortcuts dialog focuses the close control on open, traps Tab within the panel, restores focus to the previously focused element on close, and exposes `aria-describedby` for the subtitle.
[2026-04-11] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) now covers `GET /api/search` with no `q` (empty `posts`/`pages`) and `q=About` (arrays plus `/about` in `pages`).
[2026-04-12] B (CMS core): Dashboard Tags merge section sorts tags A–Z (case-insensitive), adds a search field to filter by name or slug, and keeps merge targets in sorted order in each dropdown.
[2026-04-13] C (Developer / geek): Markdown AST lab measures `parseMarkdownToMdast` with `performance.now()` in the same `useMemo` as the tree, shows **ms parse** and UTF-16 **char** count in the syntax-tree header (including failed parses), with a short tooltip on timing.
[2026-04-14] B (CMS core): Dashboard custom pages (`/dashboard/content/pages`) show **Last updated** with `formatRelativeTime` and `formatAbsoluteDateTime` in the `title` tooltip; scheduled publish uses the same absolute formatter for consistency with posts/notes.
[2026-04-15] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) now covers `GET /api/giscus-config` — 200 response and JSON includes boolean `enabled` (full config or `{ enabled: false }` when env is incomplete).
[2026-04-16] A (UX polish): Dashboard command palette adds **About page content** (`/dashboard/content/about`) in the Content section with search keywords (about, profile, bio, story, headshot, personal, intro).
[2026-04-17] B (CMS core): Dashboard audit log table **Time** column shows `formatRelativeTime` with `formatAbsoluteDateTime` in the cell `title` tooltip; CSV export and copied investigation notes still use the existing full `formatDate` strings.
[2026-04-18] D (Reliability / Infra): Jest suite `api-giscus-config.test.ts` covers `GET /api/giscus-config` — `{ enabled: false }` when env is incomplete, full payload when `GISCUS_*` is set, and `NEXT_PUBLIC_GISCUS_*` precedence over non-public keys (isolates env in beforeEach/afterEach).
[2026-04-19] A (UX polish): Keyboard shortcuts dialog lists **Ctrl+K**, **Ctrl+S**, and **Ctrl+Enter** next to the existing ⌘ chords for command palette, save, and publish (aligned with the subtitle’s Ctrl+/ note for non-macOS users).
[2026-04-20] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) now covers `GET /.well-known/security.txt` — 200, `Content-Type` matches `text/plain`, body includes `Preferred-Languages: en` (complements existing Jest route tests).
[2026-04-21] B (CMS core): New `GET /api/cv/status` (auth) uses S3 `HeadObject` on `cv.pdf` for `exists`, `lastModified`, and `contentLength`; dashboard CV page loads it (refreshes after upload) and shows **Last uploaded** with `formatRelativeTime` + `formatAbsoluteDateTime` tooltip and approximate KB; Jest `api-cv-status.test.ts` covers 401, missing object, and success.
[2026-04-22] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) asserts `GET /api/cv/status` without a session returns **401** and JSON `{ error: "Unauthorized" }` (first explicit unauthenticated API check in smoke).
[2026-04-23] C (Developer / geek): Markdown AST lab shows **lines** next to char count — `source.length === 0 ? 0 : source.split("\n").length`, with a short tooltip (LF split; empty editor = 0).
[2026-04-24] A (UX polish): Dashboard CV page shows a **loading** row (spinner + “Checking whether a CV is on file…”) while `GET /api/cv/status` is in flight, then a **no file** status message when the response is `exists: false` (instead of a silent empty state).
[2026-04-25] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) asserts `GET /api/export` without a session returns **401** and JSON `{ error: "Unauthorized" }` (CMS export must stay session-gated).
[2026-04-26] C (Developer / geek): Markdown AST lab shows **words** (trim + whitespace split; empty/whitespace-only → 0) beside chars/lines; logic lives in `src/lib/markdown-source-metrics.ts` with `countSourceLines` / `countSourceWords` and Jest in `markdown-source-metrics.test.ts`.
[2026-04-27] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) covers `GET /blog/archive` — 200, body includes “archive”, “back to blog”, and either “post(s) in total” or the empty-state “no posts available yet”.
[2026-04-28] A (UX polish): Dashboard command palette adds **Blog archive (public site)** (`/blog/archive`) in the Go section with search keywords (archive, year, chronological, timeline, browse).
[2026-04-29] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) asserts `POST /api/import` with an empty payload shape (`{ posts: [], customPages: [] }`) and **no session** returns **401** and JSON `{ error: "Unauthorized" }` (import stays session-gated like export).
[2026-04-30] A (UX polish): Dashboard command palette adds **Blog index (public site)** (`/blog`) in the Go section (BookOpen icon; keywords: reader, latest, published, posts) ahead of the existing public archive action.
[2026-05-01] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) asserts `GET /api/audit` without a session returns **401** and JSON `{ error: "Unauthorized" }` (audit log API stays session-gated).
[2026-05-02] C (Developer / geek): Markdown AST lab shows **UTF-8 bytes** next to JS char count via `countSourceUtf8Bytes` (`TextEncoder`) and a tooltip; Jest covers empty, ASCII, and emoji vs UTF-16 length.
[2026-05-03] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) **blog** check now asserts `GET /blog` returns HTML with the meta description (“read articles about engineering…”), RSS subscribe copy, and `feed.xml` link (not only status 200; tag grid subtitle stays client-only).
[2026-05-04] A (UX polish): Dashboard command palette adds **About** and **Contact** as public-site destinations (`/about`, `/contact`) in the Go section after the blog entries, with discovery keywords (profile, bio, form, reach, visitor).
[2026-05-05] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) covers `GET /api/tags` — **200** and a JSON **array** without a session, and `GET /api/tags?all=1` without a session returns **401** with `{ error: "Unauthorized" }` (dashboard-only full tag list).
[2026-05-06] A (UX polish): Dashboard command palette adds **Home page (public site)** (`/`) with a **House** icon before the other public-site shortcuts; **homepage** keyword moved from blog index to this action to separate visitor landing vs blog listing.
[2026-05-07] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) covers anonymous `GET /api/posts` — **200**, JSON **array**, each entry has **slug** and **title**, and each row is visibly public (`published: true` or `publishedAt` in the past), matching the route’s unauthenticated filter.
[2026-04-06] A (UX polish): Dashboard keyboard shortcuts dialog **Global** row now lists **Ctrl+/** alongside **⌘/** and **?** for toggling the panel (matches the subtitle and the Ctrl chord pattern used for palette / save / publish).
[2026-04-06] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) now asserts `GET /api/health` sends **Cache-Control** with **no-store**, and **HEAD /api/health** matches GET status with **no-store** and an **empty body** (readiness probe parity with v1 health and live).
[2026-04-06] C (Developer / geek): Markdown AST lab shows **graphemes** via `countSourceGraphemeClusters` (`Intl.Segmenter`, code-point fallback); Jest covers empty, ASCII, emoji vs UTF-16 length, and e+combining acute when Segmenter is available.
[2026-05-09] B (CMS core / SEO) + D (smoke): Replaced split **`generateSitemaps`** with a **single** default **`sitemap()`** so **`/sitemap.xml`** is a real **urlset** (static + public custom pages + public posts). The old split route passed **`id` as a Promise**, broke chunk matching, and left **`/sitemap.xml`** as 404 while only **`/sitemap/N.xml`** responded. **Prisma** reads are wrapped so a **down DB** (including **CI** with a dummy `DATABASE_URL`) still returns **core static URLs**. Removed an accidental empty **`app/sitemap.xml/`** directory that **shadowed** metadata and kept **`/sitemap.xml`** on a 404 page. Playwright smoke asserts **urlset** XML with **about / blog / contact** `<loc>`s.
[2026-05-10] D (Reliability / Infra): **`playwright.config.ts`** — in **CI**, **`next start`** binds **`PORT`** from **`PLAYWRIGHT_E2E_PORT`** (default **3041**) so the E2E server does not collide with **:3000**; **`use.baseURL`** resolves to that origin (or **`PLAYWRIGHT_BASE_URL`** when set); **`reuseExistingServer: false`** so runs never attach to an unrelated listener; **`webServer.url`** targets **`GET /api/live`** (no DB) so readiness does not depend on Prisma-heavy SSR. Local (non-CI) behavior stays **`http://localhost:3000`** with no managed **`webServer`**.
[2026-05-11] D (Reliability / Infra): **`.github/workflows/ci.yml`** — the **Playwright E2E** step sets **`NEXTAUTH_URL`** and **`NEXT_PUBLIC_SITE_URL`** to **`http://127.0.0.1:3041`** so runtime auth and absolute site URLs align with the CI **`playwright.config.ts`** origin (default **`PLAYWRIGHT_E2E_PORT`** **3041**); the **verify** step keeps **`localhost:3000`** for the standalone production build.
[2026-05-12] B (CMS core): Dashboard **My notes** search (`/dashboard/notes`) now matches **slug** and **description** in addition to title, body, tags, and category; the field uses **`type="search"`**, **`aria-label="Search notes"`**, and an updated placeholder describing all searchable fields.
[2026-05-13] D (Reliability / Infra): **`/auth/signin`** exports server **`metadata`** (**`Admin sign-in`** title + description) so HTML probes stay stable before client hydration; Playwright smoke (`e2e/smoke.spec.ts`) asserts **200** and **`admin sign-in`** in the document (complements redirect-only auth checks).
[2026-05-14] A (UX polish): Dashboard command palette **Go** section adds **Blog RSS feed (public site)** (`/feed.xml`, **Rss** icon) and **Sitemap XML (public site)** (`/sitemap.xml`, **Map** icon) with discovery keywords (rss, subscribe, seo, crawl, urlset).
[2026-05-15] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) **`GET /robots.txt`** now checks **`Content-Type`** **`text/plain`**, **`User-agent`**, **`Allow: /`**, **`Disallow`** for **`/dashboard/`** and **`/api/`**, and a **`Sitemap:`** URL ending in **`sitemap.xml`** (aligns with **`src/app/robots.ts`**).
[2026-05-16] A (UX polish): Dashboard command palette **Go** section adds **Robots.txt (public site)** (`/robots.txt`, **Bot** icon) with keywords (robots, crawler, spider, allow, disallow) alongside RSS and sitemap shortcuts.
[2026-05-17] C (Developer / geek): Markdown AST lab (`ast-lab-client`) computes **SHA-256** of **UTF-8** source via **`crypto.subtle.digest`** in a **`useEffect`**, shows a **truncated hex** prefix + **sha256** label in the syntax-tree metrics row, and puts the **full hash** in the **`title`** tooltip (or explains when Web Crypto is unavailable).
[2026-05-18] A (UX polish): Dashboard command palette **Go** section adds **Security.txt (public site)** (`/.well-known/security.txt`, **KeyRound** icon) with keywords (security.txt, disclosure, vulnerability, RFC, well-known, responsible).
[2026-05-19] D (Reliability / Infra): Playwright smoke (`e2e/smoke.spec.ts`) **`GET /feed.xml`** now asserts **`Content-Type`** XML + **UTF-8**, **`Cache-Control`** caching hints (**`public`** / **`s-maxage`**), XML declaration, **`<rss>`** / **`<channel>`** / **`<lastBuildDate>`** (present even with zero posts), **`feed.xml`** in the document, and **`rel="self"`** on the Atom self link (matches **`src/app/feed.xml/route.ts`**).
[2026-05-20] B (CMS core): Dashboard **published posts** table (`posts-table-client`) adds a **Slug** column (**`font-mono`**, truncated, full value in **`title`**) between title and status; mobile cards show the slug under the title; table **`min-w`** widened slightly for layout balance.
[2026-05-21] D (Reliability / Infra): **`/.well-known/security.txt`** adds explicit **`HEAD`** (**`NextResponse(null)`**, same **`Content-Type`** / **`Cache-Control`** as **`GET`**); shared header constant in the route module; Jest (`security-txt-route.test.ts`) covers **`HEAD`**; Playwright smoke asserts **`HEAD`** matches **`GET`** status and returns an **empty** body.
