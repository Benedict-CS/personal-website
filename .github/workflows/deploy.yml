# CD: Deploy to server on push to main (benedict.winlab.tw)
# Requires GitHub Secrets: DEPLOY_HOST, DEPLOY_USER, SSH_PRIVATE_KEY
# Optional: DEPLOY_PATH (default: ~/personal-website)

name: CD - Deploy to server

on:
  push:
    branches: [test]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Lint, unit tests, and production build
        run: npm run verify
        env:
          DATABASE_URL: postgresql://dummy:dummy@localhost:5432/dummy
          NEXTAUTH_SECRET: dummy-secret
          NEXTAUTH_URL: https://benedict.winlab.tw
          NEXT_PUBLIC_SITE_URL: https://benedict.winlab.tw

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    if: success()
    timeout-minutes: 30
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 20m
          script: |
            set -e
            DEPLOY_PATH="${{ secrets.DEPLOY_PATH }}"
            cd "${DEPLOY_PATH:-$HOME/personal-website}"
            CI=1 ./scripts/manual-deploy.sh
